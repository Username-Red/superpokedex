import { captureBtn, encounterBtn, getDesc, giveSprite, initializeLists, loadHeaderFooter} from "./utils.mjs";

loadHeaderFooter()
initializeLists();
// getPkmnInfo();
giveSprite("lugia");
getDesc("lugia");
encounterBtn();
captureBtn();
